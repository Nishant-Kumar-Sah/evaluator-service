export  const  PYTHON_IMAGE = "python:3.8-slim"

//This will represent the header size of the docker stream
//docker stream header will contain data about the type of stream i.e., stdout/stderr
// and length of the data
export const DOCKER_STREAM_HEADER_SIZE = 8
